diff --git a/node_modules/@replit/codemirror-minimap/dist/index.cjs b/node_modules/@replit/codemirror-minimap/dist/index.cjs
index 3f0c98a..0755eda 100644
--- a/node_modules/@replit/codemirror-minimap/dist/index.cjs
+++ b/node_modules/@replit/codemirror-minimap/dist/index.cjs
@@ -95,10 +95,14 @@ const Theme$1 = view.EditorView.theme({
     },
 });
 const SCALE = Scale.PixelMultiplier * Scale.SizeRatio;
+const MODIFIER_KEYS = new Set(["Shift", "Control", "Alt", "Meta"]);
 const OverlayView = view.ViewPlugin.fromClass(class {
     constructor(view) {
         this.view = view;
         this._isDragging = false;
+        this._wasEditable = false;
+        this._wasFocused = false;
+        this._editableToggled = false;
         if (view.state.facet(Config).enabled) {
             this.create(view);
         }
@@ -108,9 +112,24 @@ const OverlayView = view.ViewPlugin.fromClass(class {
         this.dom = crelt__default["default"]("div", { class: "cm-minimap-overlay" });
         this.container.appendChild(this.dom);
         // Attach event listeners for overlay
-        this.container.addEventListener("mousedown", this.onMouseDown.bind(this));
-        window.addEventListener("mouseup", this.onMouseUp.bind(this));
-        window.addEventListener("mousemove", this.onMouseMove.bind(this));
+        this._boundOnMouseDown = this.onMouseDown.bind(this);
+        this._boundOnMouseUp = this.onMouseUp.bind(this);
+        this._boundOnMouseMove = this.onMouseMove.bind(this);
+        this._boundOnKeyDown = this.onKeyDown.bind(this);
+        this._boundOnBlur = this._endDrag.bind(this);
+        this.container.addEventListener("mousedown", this._boundOnMouseDown);
+        window.addEventListener("mouseup", this._boundOnMouseUp);
+        window.addEventListener("mousemove", this._boundOnMouseMove);
+        window.addEventListener("keydown", this._boundOnKeyDown);
+        // A window blur mid-drag (Cmd-Tab, a Space switch, a native context
+        // menu) otherwise self-heals only on the next mousemove with the
+        // primary button released — and an inactive macOS window never
+        // gets one. Tearing down on blur too matches the rule
+        // tableHandles.ts's own drag-reorder already established for
+        // exactly this failure mode: an OS/browser-level interruption or a
+        // mid-drag app/tab switch needs its own exit path, not just the
+        // in-window ones.
+        window.addEventListener("blur", this._boundOnBlur);
         // Attach the overlay elements to the minimap
         const inner = view.dom.querySelector(".cm-minimap-inner");
         if (inner) {
@@ -123,12 +142,91 @@ const OverlayView = view.ViewPlugin.fromClass(class {
     }
     remove() {
         if (this.container) {
-            this.container.removeEventListener("mousedown", this.onMouseDown);
-            window.removeEventListener("mouseup", this.onMouseUp);
-            window.removeEventListener("mousemove", this.onMouseMove);
+            this.container.removeEventListener("mousedown", this._boundOnMouseDown);
+            window.removeEventListener("mouseup", this._boundOnMouseUp);
+            window.removeEventListener("mousemove", this._boundOnMouseMove);
+            window.removeEventListener("keydown", this._boundOnKeyDown);
+            window.removeEventListener("blur", this._boundOnBlur);
+            this._endDrag();
             this.container.remove();
         }
     }
+    // Single teardown path for every way a drag can end: a normal mouseup,
+    // the off-window self-heal in onMouseMove, a window blur, or the
+    // view/overlay being torn down mid-drag. Restoring contentEditable and
+    // the body-level guards here (rather than at each call site) is what
+    // keeps the escape hatch in onKeyDown safe to call independently
+    // without duplicating this bookkeeping, and is also what makes the
+    // re-entrancy guard at the top of onMouseDown correct: it closes out
+    // whatever drag was still latched, fully, before a fresh one starts.
+    _endDrag() {
+        if (!this._isDragging) {
+            return;
+        }
+        this._dragStartY = undefined;
+        this._isDragging = false;
+        if (this.container) {
+            this.container.classList.remove("cm-minimap-overlay-active");
+        }
+        // Unconditional, not "restore whatever was there before": this
+        // guard is shared global state across every minimap instance (one
+        // per editor pane in a split view), and "restore what I captured"
+        // is unpaired the moment one instance's drag ends without its own
+        // matching capture ever landing (e.g. a stale latched drag on
+        // another pane) — the wrong "previous" value gets written back and
+        // then perpetuated by every drag after it, with body stuck at
+        // cursor:default/user-select:none until a reload. Blanking is the
+        // only pairing that's correct no matter how many instances exist.
+        document.body.style.cursor = "";
+        document.body.style.userSelect = "";
+        document.body.style.webkitUserSelect = "";
+        this._restoreEditability();
+    }
+    // WebKit's effectiveUserSelect deliberately ignores an inherited
+    // `user-select: none` on any element carrying `-webkit-user-modify:
+    // read-write*` (see WebKit changeset 293028) — which is exactly what
+    // CodeMirror's own baseTheme sets on `.cm-content` whenever it's
+    // contenteditable. That's why the user-select guards above never
+    // stopped a drag that crossed from the minimap into the editor text:
+    // they guard elements WebKit exempts from the guard. Temporarily
+    // clearing contentEditable removes that exemption for the drag's
+    // duration; restoring it here (and refocusing, if the view was
+    // focused) hands control back to CodeMirror without ever touching its
+    // own EditorState, so the caret, selection, and scroll position are
+    // exactly what they were before the toggle. `observer.ignore` matches
+    // how CodeMirror's own updateAttrs touches this same attribute — it
+    // stops the DOMObserver's MutationObserver for the duration of the
+    // write so this doesn't needlessly mark the doc view dirty.
+    _restoreEditability() {
+        if (this._wasEditable) {
+            this.view.observer.ignore(() => {
+                this.view.contentDOM.setAttribute("contenteditable", "true");
+            });
+            if (this._wasFocused && document.contains(this.view.contentDOM)) {
+                this.view.focus();
+            }
+        }
+        this._wasEditable = false;
+        this._wasFocused = false;
+        this._editableToggled = false;
+    }
+    // Doesn't rescue the keystroke that triggered this: clearing
+    // contenteditable already blurred `.cm-content`, so this keydown
+    // dispatched against a non-editable, unfocused element and
+    // CodeMirror's own input handling never saw it — refocusing here
+    // can't retarget an event already in flight. What it does do is put
+    // editability and focus back before the *next* one, so typing resumes
+    // normally instead of staying silently stuck for the rest of the
+    // drag. A bare modifier isn't a signal to act on here: nothing was
+    // going to be inserted by it, and firing on every Shift/Cmd tap would
+    // needlessly re-expose `.cm-content` to the WebKit exemption for the
+    // rest of the gesture.
+    onKeyDown(event) {
+        if (!this._isDragging || MODIFIER_KEYS.has(event.key)) {
+            return;
+        }
+        this._restoreEditability();
+    }
     update(update) {
         const prev = update.startState.facet(Config).enabled;
         const now = update.state.facet(Config).enabled;
@@ -192,41 +290,85 @@ const OverlayView = view.ViewPlugin.fromClass(class {
         if (!this.container) {
             return;
         }
-        // Ignore right click
-        if (event.button === 2) {
+        // A mousedown arriving while a drag is still latched (its mouseup
+        // never reached us) must close out the stale drag first. Without
+        // this, a fresh mousedown would fall straight through to the
+        // capture below while `.cm-content` is already contenteditable
+        // "false" from the earlier drag, recording `_wasEditable = false`
+        // and permanently skipping the restore — the editor keeps focus
+        // and silently drops every keystroke until the file is closed and
+        // reopened. Routing through _endDrag() here instead restores
+        // correctly, using whatever was captured when the *first* drag
+        // actually toggled it.
+        if (this._isDragging) {
+            this._endDrag();
+        }
+        // Only the primary (left) button starts a drag; right- and
+        // middle-click are left untouched.
+        if (event.button !== 0) {
             return;
         }
-        // If target is the overlay start dragging
+        event.preventDefault();
         const { clientY, target } = event;
-        if (target === this.dom) {
-            this._dragStartY = event.clientY;
-            this._isDragging = true;
-            this.container.classList.add("cm-minimap-overlay-active");
-            return;
-        }
-        // Updates the scroll position of the EditorView based on the
-        // position of the MouseEvent on the minimap canvas
-        const { clientHeight, scrollHeight, scrollTop } = this.view.scrollDOM;
-        const targetTop = target.getBoundingClientRect().top;
-        const deltaY = (clientY - targetTop) * SCALE;
-        const scrollRatio = scrollTop / (scrollHeight - clientHeight);
-        const visibleRange = clientHeight * SCALE - clientHeight;
-        const visibleTop = visibleRange * scrollRatio;
-        const top = Math.max(0, scrollTop - visibleTop);
-        this.view.scrollDOM.scrollTop = top + deltaY - clientHeight / 2;
+        if (target !== this.dom) {
+            // Updates the scroll position of the EditorView based on the
+            // position of the MouseEvent on the minimap canvas
+            const { clientHeight, scrollHeight, scrollTop } = this.view.scrollDOM;
+            const targetTop = target.getBoundingClientRect().top;
+            const deltaY = (clientY - targetTop) * SCALE;
+            const scrollRatio = scrollTop / (scrollHeight - clientHeight);
+            const visibleRange = clientHeight * SCALE - clientHeight;
+            const visibleTop = visibleRange * scrollRatio;
+            const top = Math.max(0, scrollTop - visibleTop);
+            this.view.scrollDOM.scrollTop = top + deltaY - clientHeight / 2;
+            // Re-anchor the indicator to the jumped-to position before
+            // engaging drag-tracking below: computeTop() no-ops once
+            // _isDragging is set, and onMouseMove's own delta math reads
+            // this.dom.style.top, so a stale value here throws the jump
+            // away on the very first pointer move of the gesture.
+            this.computeTop();
+        }
+        // Any primary-button mousedown inside the container starts a drag,
+        // whether it lands on the viewport indicator or the canvas/track.
+        this._dragStartY = event.clientY;
+        this._isDragging = true;
+        this.container.classList.add("cm-minimap-overlay-active");
+        document.body.style.cursor = "default";
+        document.body.style.userSelect = "none";
+        document.body.style.webkitUserSelect = "none";
+        // contentEditable is deliberately NOT touched here — see the check
+        // at the top of onMouseMove for why.
     }
     onMouseUp(_event) {
         // Stop dragging on mouseup
-        if (this._isDragging && this.container) {
-            this._dragStartY = undefined;
-            this._isDragging = false;
-            this.container.classList.remove("cm-minimap-overlay-active");
-        }
+        this._endDrag();
     }
     onMouseMove(event) {
+        if (this._isDragging && (event.buttons & 1) === 0) {
+            this.onMouseUp(event);
+            return;
+        }
         if (!this._isDragging || !this.dom) {
             return;
         }
+        // WebKit only exempts `.cm-content` from the user-select/cursor
+        // guards set in onMouseDown once the pointer is actually over it —
+        // so clearing contenteditable is only needed once a drag reaches
+        // outside the minimap, not for a plain click-to-jump or a drag
+        // that never leaves the track. Deferring the check to here (once
+        // per drag, via _editableToggled) means those common cases never
+        // blur `.cm-content`, so the native caret never disappears for
+        // them.
+        if (!this._editableToggled && this.container && event.target instanceof Node && !this.container.contains(event.target)) {
+            this._editableToggled = true;
+            this._wasEditable = this.view.contentDOM.getAttribute("contenteditable") === "true";
+            this._wasFocused = this.view.hasFocus;
+            if (this._wasEditable) {
+                this.view.observer.ignore(() => {
+                    this.view.contentDOM.setAttribute("contenteditable", "false");
+                });
+            }
+        }
         event.preventDefault();
         event.stopPropagation();
         // Without an existing position, we're just beginning to drag.
diff --git a/node_modules/@replit/codemirror-minimap/dist/index.js b/node_modules/@replit/codemirror-minimap/dist/index.js
index c7c9a04..98b98ac 100644
--- a/node_modules/@replit/codemirror-minimap/dist/index.js
+++ b/node_modules/@replit/codemirror-minimap/dist/index.js
@@ -87,10 +87,14 @@ const Theme$1 = /*@__PURE__*/EditorView.theme({
     },
 });
 const SCALE = Scale.PixelMultiplier * Scale.SizeRatio;
+const MODIFIER_KEYS = new Set(["Shift", "Control", "Alt", "Meta"]);
 const OverlayView = /*@__PURE__*/ViewPlugin.fromClass(class {
     constructor(view) {
         this.view = view;
         this._isDragging = false;
+        this._wasEditable = false;
+        this._wasFocused = false;
+        this._editableToggled = false;
         if (view.state.facet(Config).enabled) {
             this.create(view);
         }
@@ -100,9 +104,24 @@ const OverlayView = /*@__PURE__*/ViewPlugin.fromClass(class {
         this.dom = crelt("div", { class: "cm-minimap-overlay" });
         this.container.appendChild(this.dom);
         // Attach event listeners for overlay
-        this.container.addEventListener("mousedown", this.onMouseDown.bind(this));
-        window.addEventListener("mouseup", this.onMouseUp.bind(this));
-        window.addEventListener("mousemove", this.onMouseMove.bind(this));
+        this._boundOnMouseDown = this.onMouseDown.bind(this);
+        this._boundOnMouseUp = this.onMouseUp.bind(this);
+        this._boundOnMouseMove = this.onMouseMove.bind(this);
+        this._boundOnKeyDown = this.onKeyDown.bind(this);
+        this._boundOnBlur = this._endDrag.bind(this);
+        this.container.addEventListener("mousedown", this._boundOnMouseDown);
+        window.addEventListener("mouseup", this._boundOnMouseUp);
+        window.addEventListener("mousemove", this._boundOnMouseMove);
+        window.addEventListener("keydown", this._boundOnKeyDown);
+        // A window blur mid-drag (Cmd-Tab, a Space switch, a native context
+        // menu) otherwise self-heals only on the next mousemove with the
+        // primary button released — and an inactive macOS window never
+        // gets one. Tearing down on blur too matches the rule
+        // tableHandles.ts's own drag-reorder already established for
+        // exactly this failure mode: an OS/browser-level interruption or a
+        // mid-drag app/tab switch needs its own exit path, not just the
+        // in-window ones.
+        window.addEventListener("blur", this._boundOnBlur);
         // Attach the overlay elements to the minimap
         const inner = view.dom.querySelector(".cm-minimap-inner");
         if (inner) {
@@ -115,12 +134,91 @@ const OverlayView = /*@__PURE__*/ViewPlugin.fromClass(class {
     }
     remove() {
         if (this.container) {
-            this.container.removeEventListener("mousedown", this.onMouseDown);
-            window.removeEventListener("mouseup", this.onMouseUp);
-            window.removeEventListener("mousemove", this.onMouseMove);
+            this.container.removeEventListener("mousedown", this._boundOnMouseDown);
+            window.removeEventListener("mouseup", this._boundOnMouseUp);
+            window.removeEventListener("mousemove", this._boundOnMouseMove);
+            window.removeEventListener("keydown", this._boundOnKeyDown);
+            window.removeEventListener("blur", this._boundOnBlur);
+            this._endDrag();
             this.container.remove();
         }
     }
+    // Single teardown path for every way a drag can end: a normal mouseup,
+    // the off-window self-heal in onMouseMove, a window blur, or the
+    // view/overlay being torn down mid-drag. Restoring contentEditable and
+    // the body-level guards here (rather than at each call site) is what
+    // keeps the escape hatch in onKeyDown safe to call independently
+    // without duplicating this bookkeeping, and is also what makes the
+    // re-entrancy guard at the top of onMouseDown correct: it closes out
+    // whatever drag was still latched, fully, before a fresh one starts.
+    _endDrag() {
+        if (!this._isDragging) {
+            return;
+        }
+        this._dragStartY = undefined;
+        this._isDragging = false;
+        if (this.container) {
+            this.container.classList.remove("cm-minimap-overlay-active");
+        }
+        // Unconditional, not "restore whatever was there before": this
+        // guard is shared global state across every minimap instance (one
+        // per editor pane in a split view), and "restore what I captured"
+        // is unpaired the moment one instance's drag ends without its own
+        // matching capture ever landing (e.g. a stale latched drag on
+        // another pane) — the wrong "previous" value gets written back and
+        // then perpetuated by every drag after it, with body stuck at
+        // cursor:default/user-select:none until a reload. Blanking is the
+        // only pairing that's correct no matter how many instances exist.
+        document.body.style.cursor = "";
+        document.body.style.userSelect = "";
+        document.body.style.webkitUserSelect = "";
+        this._restoreEditability();
+    }
+    // WebKit's effectiveUserSelect deliberately ignores an inherited
+    // `user-select: none` on any element carrying `-webkit-user-modify:
+    // read-write*` (see WebKit changeset 293028) — which is exactly what
+    // CodeMirror's own baseTheme sets on `.cm-content` whenever it's
+    // contenteditable. That's why the user-select guards above never
+    // stopped a drag that crossed from the minimap into the editor text:
+    // they guard elements WebKit exempts from the guard. Temporarily
+    // clearing contentEditable removes that exemption for the drag's
+    // duration; restoring it here (and refocusing, if the view was
+    // focused) hands control back to CodeMirror without ever touching its
+    // own EditorState, so the caret, selection, and scroll position are
+    // exactly what they were before the toggle. `observer.ignore` matches
+    // how CodeMirror's own updateAttrs touches this same attribute — it
+    // stops the DOMObserver's MutationObserver for the duration of the
+    // write so this doesn't needlessly mark the doc view dirty.
+    _restoreEditability() {
+        if (this._wasEditable) {
+            this.view.observer.ignore(() => {
+                this.view.contentDOM.setAttribute("contenteditable", "true");
+            });
+            if (this._wasFocused && document.contains(this.view.contentDOM)) {
+                this.view.focus();
+            }
+        }
+        this._wasEditable = false;
+        this._wasFocused = false;
+        this._editableToggled = false;
+    }
+    // Doesn't rescue the keystroke that triggered this: clearing
+    // contenteditable already blurred `.cm-content`, so this keydown
+    // dispatched against a non-editable, unfocused element and
+    // CodeMirror's own input handling never saw it — refocusing here
+    // can't retarget an event already in flight. What it does do is put
+    // editability and focus back before the *next* one, so typing resumes
+    // normally instead of staying silently stuck for the rest of the
+    // drag. A bare modifier isn't a signal to act on here: nothing was
+    // going to be inserted by it, and firing on every Shift/Cmd tap would
+    // needlessly re-expose `.cm-content` to the WebKit exemption for the
+    // rest of the gesture.
+    onKeyDown(event) {
+        if (!this._isDragging || MODIFIER_KEYS.has(event.key)) {
+            return;
+        }
+        this._restoreEditability();
+    }
     update(update) {
         const prev = update.startState.facet(Config).enabled;
         const now = update.state.facet(Config).enabled;
@@ -184,41 +282,85 @@ const OverlayView = /*@__PURE__*/ViewPlugin.fromClass(class {
         if (!this.container) {
             return;
         }
-        // Ignore right click
-        if (event.button === 2) {
+        // A mousedown arriving while a drag is still latched (its mouseup
+        // never reached us) must close out the stale drag first. Without
+        // this, a fresh mousedown would fall straight through to the
+        // capture below while `.cm-content` is already contenteditable
+        // "false" from the earlier drag, recording `_wasEditable = false`
+        // and permanently skipping the restore — the editor keeps focus
+        // and silently drops every keystroke until the file is closed and
+        // reopened. Routing through _endDrag() here instead restores
+        // correctly, using whatever was captured when the *first* drag
+        // actually toggled it.
+        if (this._isDragging) {
+            this._endDrag();
+        }
+        // Only the primary (left) button starts a drag; right- and
+        // middle-click are left untouched.
+        if (event.button !== 0) {
             return;
         }
-        // If target is the overlay start dragging
+        event.preventDefault();
         const { clientY, target } = event;
-        if (target === this.dom) {
-            this._dragStartY = event.clientY;
-            this._isDragging = true;
-            this.container.classList.add("cm-minimap-overlay-active");
-            return;
-        }
-        // Updates the scroll position of the EditorView based on the
-        // position of the MouseEvent on the minimap canvas
-        const { clientHeight, scrollHeight, scrollTop } = this.view.scrollDOM;
-        const targetTop = target.getBoundingClientRect().top;
-        const deltaY = (clientY - targetTop) * SCALE;
-        const scrollRatio = scrollTop / (scrollHeight - clientHeight);
-        const visibleRange = clientHeight * SCALE - clientHeight;
-        const visibleTop = visibleRange * scrollRatio;
-        const top = Math.max(0, scrollTop - visibleTop);
-        this.view.scrollDOM.scrollTop = top + deltaY - clientHeight / 2;
+        if (target !== this.dom) {
+            // Updates the scroll position of the EditorView based on the
+            // position of the MouseEvent on the minimap canvas
+            const { clientHeight, scrollHeight, scrollTop } = this.view.scrollDOM;
+            const targetTop = target.getBoundingClientRect().top;
+            const deltaY = (clientY - targetTop) * SCALE;
+            const scrollRatio = scrollTop / (scrollHeight - clientHeight);
+            const visibleRange = clientHeight * SCALE - clientHeight;
+            const visibleTop = visibleRange * scrollRatio;
+            const top = Math.max(0, scrollTop - visibleTop);
+            this.view.scrollDOM.scrollTop = top + deltaY - clientHeight / 2;
+            // Re-anchor the indicator to the jumped-to position before
+            // engaging drag-tracking below: computeTop() no-ops once
+            // _isDragging is set, and onMouseMove's own delta math reads
+            // this.dom.style.top, so a stale value here throws the jump
+            // away on the very first pointer move of the gesture.
+            this.computeTop();
+        }
+        // Any primary-button mousedown inside the container starts a drag,
+        // whether it lands on the viewport indicator or the canvas/track.
+        this._dragStartY = event.clientY;
+        this._isDragging = true;
+        this.container.classList.add("cm-minimap-overlay-active");
+        document.body.style.cursor = "default";
+        document.body.style.userSelect = "none";
+        document.body.style.webkitUserSelect = "none";
+        // contentEditable is deliberately NOT touched here — see the check
+        // at the top of onMouseMove for why.
     }
     onMouseUp(_event) {
         // Stop dragging on mouseup
-        if (this._isDragging && this.container) {
-            this._dragStartY = undefined;
-            this._isDragging = false;
-            this.container.classList.remove("cm-minimap-overlay-active");
-        }
+        this._endDrag();
     }
     onMouseMove(event) {
+        if (this._isDragging && (event.buttons & 1) === 0) {
+            this.onMouseUp(event);
+            return;
+        }
         if (!this._isDragging || !this.dom) {
             return;
         }
+        // WebKit only exempts `.cm-content` from the user-select/cursor
+        // guards set in onMouseDown once the pointer is actually over it —
+        // so clearing contenteditable is only needed once a drag reaches
+        // outside the minimap, not for a plain click-to-jump or a drag
+        // that never leaves the track. Deferring the check to here (once
+        // per drag, via _editableToggled) means those common cases never
+        // blur `.cm-content`, so the native caret never disappears for
+        // them.
+        if (!this._editableToggled && this.container && event.target instanceof Node && !this.container.contains(event.target)) {
+            this._editableToggled = true;
+            this._wasEditable = this.view.contentDOM.getAttribute("contenteditable") === "true";
+            this._wasFocused = this.view.hasFocus;
+            if (this._wasEditable) {
+                this.view.observer.ignore(() => {
+                    this.view.contentDOM.setAttribute("contenteditable", "false");
+                });
+            }
+        }
         event.preventDefault();
         event.stopPropagation();
         // Without an existing position, we're just beginning to drag.
