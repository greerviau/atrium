{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. Grants only the app's own hand-written commands (default for #[tauri::command], no plugin scope needed), the folder picker, PR-link opening, Reveal in Finder, OS clipboard read/write, and starting a window drag from the custom title bar.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://github.com/*" }]
    },
    "opener:allow-reveal-item-in-dir",
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text"
  ]
}
