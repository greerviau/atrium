# This rendered Markdown heading is deliberately long enough to wrap across several visual rows in the editor pane so the End key places the caret at a visual wrap boundary rather than at the end of the underlying source line and reproduces the WebKit focus bounce.

The terminal must retain focus after the rendered preview updates.
