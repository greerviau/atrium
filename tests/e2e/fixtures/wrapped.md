# wrapped markdown lines stay editable when the caret is after the trailing space and the rendered paragraph continues far enough to wrap across several visual rows in the editor window 

This paragraph also wraps so the fixture exercises the same rendered layout as the reported failure, including a trailing space on the heading line. 
