# This rendered Markdown heading is deliberately long enough to wrap across several visual lines in a narrow editor pane while keeping the caret at the source line end after trailing whitespace.  

The terminal click should return the heading to its fully rendered form.
